---
target: app
total_score: 26
p0_count: 0
p1_count: 3
timestamp: 2026-06-06T21-08-13Z
slug: app
---
## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Skeletons, status pills, and loading overlay all present; no prefers-reduced-motion fallback on any animation |
| 2 | Match System / Real World | 3 | Clear domain language; "System limitations", "Cluster Planner", "Custom Curations" lean technical for non-admin users |
| 3 | User Control and Freedom | 2 | No undo; browser confirm() for destructive user deletion; no form/draft recovery |
| 4 | Consistency and Standards | 3 | Mostly cohesive; 3 side-tab violations in cluster UI; hardcoded platform colors break one-accent rule in the add-project modal |
| 5 | Error Prevention | 3 | Disabled states and form validation present; browser confirm() for deletion is weaker than a designed confirmation |
| 6 | Recognition Rather Than Recall | 3 | Labeled nav items; admin plan fields lack inline definitions |
| 7 | Flexibility and Efficiency | 2 | No keyboard shortcuts for any primary action; no bulk generate; expert users hit a ceiling fast |
| 8 | Aesthetic and Minimalist Design | 3 | Dark workspace is clean and focused; uppercase sidebar eyebrows and side-tab borders add low-signal noise |
| 9 | Error Recovery | 3 | Specific, actionable error strings; graceful auth handling; no state preservation on form loss |
| 10 | Help and Documentation | 1 | Tutorial link in nav; zero contextual help for any feature; unexplained admin fields |
| **Total** | | **26/40** | **Acceptable** |

## Anti-Patterns Verdict

LLM assessment: Genuine design identity — no gradient text, no SaaS cream, no hero metrics. The dark-first workspace with one ember accent is correctly applied. The typography split (Cormorant Garamond for content, Inter for UI) is disciplined. Remaining tells: (1) side-tab borders in cluster UI at lines 4971, 5011, 5075 in page.module.css; (2) uppercase tracked sidebar eyebrow labels ("WORKSPACE", "ADMIN", "ACCOUNT"); (3) hardcoded WordPress blue and Shopify green in the add-project modal inline styles; (4) browser confirm() on admin user deletion path.

Deterministic scan: 10 findings. Genuine violations: border-left on clusterPillarBox (4971), clusterTopicItem (5011), clusterPlanFeedback (5075). False positives: border-right: 1px solid hairline divider (3439). Borderline: blockquote left-border in articleProseMirror and articleReadonlyArticleHtml — editorial convention but should be visually softer. Layout transitions: transition: width in dashboard.module.css (1041) and page.module.css (7505) should convert to transform: scaleX(). Em-dash finding in layout.tsx is metadata strings only — false positive for UI critique.

## Overall Impression

The system has a genuine design identity. The gap is operational: three banned patterns persisted into the cluster UI, the help layer is entirely absent, and power-user efficiency has not been addressed. Fixing the side-tab violations and adding contextual help would push this from acceptable to good.

## What's Working

1. Dark-first workspace with genuine restraint. Void-to-coal surface stack, single ember accent, flat elevation — executing as designed.
2. Status feedback and loading states. Skeleton states, generation overlay, status pills throughout the article list.
3. Token architecture and responsive layout. --aa-* system is organized and consistently applied. Sidebar collapse and sticky/scroll layout are correctly structured.

## Priority Issues

[P1] Side-tab borders in Cluster Planner UI — border-left on clusterPillarBox (4971), clusterTopicItem (5011), clusterPlanFeedback (5075). Fix: replace left-border on pillarBox with full border + background tint; remove left accent on topic items (background hover only); replace feedback callout border with leading icon or tinted panel. Suggested: /impeccable polish

[P1] No keyboard shortcuts or power-user efficiency paths — no shortcuts for generate, publish, navigate, close modal. Fix: Cmd/Ctrl+Enter for primary submit, Esc for modal close, G for generate in project context. Suggested: /impeccable shape keyboard shortcuts

[P1] Browser confirm() for destructive user deletion — deleteUser() calls window.confirm() in dashboard/page.tsx. Fix: replace with designed confirmation modal naming the user, ember primary CTA, toast on success. Suggested: /impeccable harden

[P2] Uppercase tracked sidebar section labels — "WORKSPACE", "ADMIN", "ACCOUNT" as all-caps tracked eyebrows on sidebarTitle. Fix: remove entirely; use hairline separators if grouping is needed. Suggested: /impeccable polish

[P2] No contextual help anywhere — admin plan fields undefined, projects empty state is just "No projects yet." Fix: add inline tooltip icons on admin labels; redesign empty state with explanation and CTA. Suggested: /impeccable clarify

## Persona Red Flags

Alex (Power User): No keyboard shortcuts for any action. Project cards are div role="link" not anchor elements. Redundant Refresh button alongside auto-poll.

Jordan (First-Timer): Empty state is a single muted line with no guidance. Platform selection step explains connection method but not product impact. "Cluster Planner" in plan description is unexplained.

Sam (Accessibility): Project cards are div[role="link"] not anchor elements. No prefers-reduced-motion alternatives in globals.css for any animation. var(--aa-accent) used in articleEditor.module.css is undefined — silent visual failure in image skeleton.

## Minor Observations

--aa-r-lg: 4px in globals.css vs 12px in DESIGN.md — spec drift. var(--aa-accent) in articleEditor.module.css is undefined; replace with var(--aa-primary). authInfo panel uses rgba(109,94,252,...) purple — undocumented second accent; replace with ember or neutral callout. aaPipelineProgressFill gradient is a justified exception; document in DESIGN.md. transition: width at dashboard.module.css:1041 and page.module.css:7505 should convert to transform: scaleX().

## Questions to Consider

What if the dashboard entry point for a first-time user was a project creation wizard, not an empty list?
The sidebar eyebrow labels group items already grouped by position — what information disappears if you remove them?
What would the keyboard shortcut layer look like if it were a first-class feature rather than an afterthought?
